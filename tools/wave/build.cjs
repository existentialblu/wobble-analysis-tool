const fs=require('node:fs'),path=require('node:path');
const dir=__dirname,dest=process.argv[2]?path.resolve(process.argv[2]):path.resolve(dir,path.basename(dir)==='source'?'..':'../../public/wave');fs.mkdirSync(dest,{recursive:true});let html=fs.readFileSync(path.join(dir,'template.html'),'utf8');for(const [key,file] of [['CSS','style.css'],['ENGINE','engine.js'],['WORKER','worker.js'],['UI','ui.js']]){const text=fs.readFileSync(path.join(dir,file),'utf8');if(key!=='CSS'&&/<\/script/i.test(text))throw Error('Unsafe inline script closing tag');html=html.replace('__'+key+'__',()=>text)}if(/__(CSS|ENGINE|WORKER|UI)__/.test(html))throw Error('Unfilled template');fs.writeFileSync(path.join(dest,'index.html'),html);console.log('Built standalone WAVE: '+Buffer.byteLength(html)+' bytes');
